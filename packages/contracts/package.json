{
  "name": "@eivo/contracts",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && nest build",
    "clean": "rimraf ./dist"
  },
  "dependencies": {
    "@automapper/classes": "^8.8.1",
    "@nestjs/mapped-types": "*",
    "@nestjs/swagger": "^11.0.6"
  },
  "devDependencies": {
    "rimraf": "~6.0.0",
    "typescript": "^5.7.3"
  }
}
