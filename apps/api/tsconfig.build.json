{
  /* Extend the config that contains the path aliases. */
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "../../dist/apps/api",
    "noEmit": false
  },
  "include": ["src/**/*", "src/*"],
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"],
}
